export const MAP_TOP_MARGIN = 32;
export const MAP_LEFT_MARGIN = 0;
export const FIELD_SIZE = [16, 13]; // field size in units; TODO move to stages
export const UNIT_SIZE = [16, 16]; // unit size in pixels
